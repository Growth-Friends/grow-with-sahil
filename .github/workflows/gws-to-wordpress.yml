name: 🚀 Deploy website on push

on:
  push: # Deploy on push to the production branch
    branches:
      - production
  workflow_dispatch: # Enables the manual trigger

jobs:
  web-deploy:
    name: 🎉 Build, Rename, and Commit
    runs-on: ubuntu-latest

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: Install Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '18.17.0'  # Ensure Node.js version meets your project's requirements

    - name: Install dependencies
      run: npm install

    - name: Build project
      run: npm run build

    - name: Rename the out folder to dalal
      run: mv out dalal

    - name: Commit the dalal folder
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add dalal
        git commit -m "Auto-commit: rename and add dalal folder after build"
        git push origin production # Push to the production branch

  ftp-deploy:
    name: 🎉 FTP Deploy
    runs-on: ubuntu-latest
    needs: web-deploy
    steps:
    - name: 🚚 Get latest code with dalal folder
      uses: actions/checkout@v4

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ftp://217.21.94.151
        username: u251186022
        password: ${{ secrets.FTP_PASSWORD }} # Store your FTP password as a secret in GitHub
        local-dir: ./dalal/ # Upload the contents of the dalal folder
        server-dir: /public_html/ # Must end with a trailing slash
        protocol: ftps # Use secure FTPS protocol
        port: 21
        log-level: standard

  cleanup:
    name: 🧹 Cleanup
    runs-on: ubuntu-latest
    needs: ftp-deploy
    steps:
    - name: Clean up build artifacts
      run: rm -rf dalal dalal.zip

    - name: Final cleanup (optional)
      if: always()
      run: |
        echo "Clean up done, no commit to repo."
